// Hook removido — reset de senha agora usa o sistema nativo do PocketBase:
// pb.collection('users').requestPasswordReset(email) no frontend.
// Este arquivo existe apenas para compatibilidade de registro do Skip Cloud.
